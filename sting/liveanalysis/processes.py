
# File containing functions that launch different process and manages
# the data sharing between them
import pathlib
from pathlib import Path
from typing import Union

def start_live_experiment(filename: Union[str, pathlib.Path]):
    """
    
    Function that starts the live experiment and 
    manages all the processes

    Args:
        filename:

    """
    pass

def start_simlive_experiment(filename: Union[str, pathlib.Path]):
    pass

